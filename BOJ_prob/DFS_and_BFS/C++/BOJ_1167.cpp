#include <iostream>
#include <list>

using namespace std;

int vertex_num;
list<list<int>> tree;


int main()
{
    cin >> vertex_num;
    list<int>::iterator iter;
    for(iter = tree.begin(); iter != tree.end(); iter++){
        edgeinfo = 0;
        while(edgeinfo != -1){
            cin >> edgeinfo;
            iter.push_back(edgeinfo);
        }
    }
}